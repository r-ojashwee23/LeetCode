Approach - 1: Character Matching(6ms)

class Solution {
    public boolean isPalindrome(int x) {
        String s=String.valueOf(x);
        if(s.charAt(0)=='-'){
            return false;
        }
        for(int i=0; i<s.length()/2; i++){
            if(s.charAt(i)!=s.charAt(s.length()-1-i)){
                return false;
            }
        }
        return true;
    }
}


Approach - 2: Reversing of digits(4ms)

class Solution {
    public boolean isPalindrome(int x) {
        if(x<0){
            return false;
        }
        if(x!=0 && x%10==0){
            return false;
        }
        int rev=0;
        while(x>rev){
            rev=rev*10+x%10;
            x/=10;
        }
        if(rev==x || x==rev/10){
            return true;
        }
        return false;
    }
}
